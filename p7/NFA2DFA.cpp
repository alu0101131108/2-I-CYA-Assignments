#include "DFA.cpp"

int main(int argc, char** argv)
{
  // ifstream ifs;
  // ifs.open(argv[1]);
  // NFA nfa1;
  // nfa1.readFromFile(ifs);
  // ifs.close();
  //
  // ofstream ofs;
  // ofs.open(argv[2]);
  // nfa1.generateDFAfromSubsetAlgorithm(ofs);
  // ofs.close();


  // ifstream ifs;
  // ifs.open(argv[1]);
  // DFA dfa1;
  // dfa1.readFromFile(ifs);
  // ifs.close();
  //
  // ofstream ofs;
  // ofs.open(argv[2]);
  // dfa1.generateDotFile(ofs);
  // ofs.close();

  return 0;
}
